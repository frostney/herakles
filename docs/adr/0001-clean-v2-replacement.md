# Clean v2 Replacement

Herakles v2 is a clean replacement for the v1 YAML/tree-based library API because the new product model is built around explicit tracked-project config, inferred hosted facts, Bun/TypeScript, and a local UI over shared core services. Preserving backward compatibility would keep the old configured tree model alive in the places where v2 most needs a simpler resolved project model.
