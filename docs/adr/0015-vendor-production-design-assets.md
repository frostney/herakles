# Vendor Production Design Assets

Herakles Workbench vendors only the production design-system assets it owns at runtime: theme tokens adapted into Tailwind, self-hosted fonts, texture CSS, and selected crochet images used by real UI states. The generated design-system bundle, prototype component examples, vendored React files, card HTML, and sample UI-kit apps remain references rather than production dependencies.
